package ui.shapes
{
    /** A Disk represents a circle filled with a uniform color. */
    public class SpikeDisk extends SpikeRing
    {
        public function SpikeDisk(radius:Number, color:uint=0xffffff)
        {
            super(0, radius, color);
        }
    }
}
